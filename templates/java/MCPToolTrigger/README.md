# MCP Tool Trigger - Java Template

This template exposes MCP tool endpoints with the Azure Functions MCP extension (preview).

Notes:
- Uses the preview extension bundle. Not for production.
- Configure mcp settings in `host.json` under `extensions.mcp`.
